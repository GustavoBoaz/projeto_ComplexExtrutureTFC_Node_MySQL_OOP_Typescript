import React from 'react';

const LoginBtn = () => (
  <button
    className="navigation-btn"
    data-testid="header__login_btn"
    type="button"
  >
    Login
  </button>
);

export default LoginBtn;
